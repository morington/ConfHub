dynaconf_merge: '@bool True'

development:
  # Development Configuration
  
  postgresql:
    scheme: '@str postgresql+asyncpg'
    port: '@int 5432'
    user: '@str ghost'
    password: '@str qwerty'
    path: '@str database'

release:
  # Release configuration
  
  postgresql:
    scheme: '@str postgresql+asyncpg'
    port: '@int 0000'
    user: '@str '
    password: '@str '
    path: '@str '
