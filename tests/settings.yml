dynaconf_merge: '@bool True'
DEV: '@bool True'

development:
  # Development Configuration
  
  postgresql:
    host: '@str 127.0.0.1'

release:
  # Release configuration
  
  postgresql:
    host: '@str 0.0.0.0'
